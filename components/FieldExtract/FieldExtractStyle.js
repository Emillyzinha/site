import { StyleSheet } from "react-native";

const styleFieldsExtract = StyleSheet.create({
    screen: {
        flex: 1,
        display: 'flex',
        paddingTop: 50,
        padding: 20,
        alignItems: "center",
    },

})

export default styleFieldsExtract
